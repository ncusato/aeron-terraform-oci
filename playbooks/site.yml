---
# Aeron deployment and benchmark setup
# Variables come from group_vars/all.yml and Terraform extra vars (-e)
- name: Aeron Deployment
  hosts: all
  become: true
  roles:
    - role: aeron-install
      tags: [install, aeron]
    - role: aeron-benchmark
      tags: [benchmark]
